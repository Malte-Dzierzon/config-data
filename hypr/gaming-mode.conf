# Gaming Mode Submap
# When active, most keybindings are disabled to prevent conflicts with game keybinds.
# Toggle with SUPER + G, or it auto-activates when Deadlock is focused.

submap {
    name = "gaming"
    # Empty submap = no keybindings active, preventing conflicts with game controls
}
